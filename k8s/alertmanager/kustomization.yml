apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - alertmanager-config.yml
  - discord-relay.yml
# helm-values-snippet.yml is intentionally NOT a resource — it's reference
# documentation for the kube-prometheus-stack HelmRelease, not an apply-able
# manifest.
